# ID Number Matching - Visual Flow Diagram

## 🎯 System Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    LOST AND FOUND SYSTEM                        │
│                  ID NUMBER MATCHING FEATURE                     │
└─────────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════
SCENARIO 1: USER REPORTS LOST ID
════════════════════════════════════════════════════════════════════

    ┌──────────────────────┐
    │  User Reports Lost   │
    │  "I lost my ID"      │
    │  ID: 221-01618       │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────────────────────┐
    │  System Receives Report              │
    │  type: "lost"                        │
    │  student_id: "221-01618"             │
    └──────────┬───────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────┐
    │  ID NUMBER MATCHING TRIGGER          │
    │  ✅ NEW FEATURE                      │
    └──────────┬───────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────┐
    │  Query Users Table                   │
    │  WHERE id_number = "221-01618"       │
    └──────────┬───────────────────────────┘
               │
        ┌──────┴──────┐
        │             │
        ▼             ▼
   ┌─────────┐   ┌──────────────┐
   │ FOUND   │   │ NOT FOUND    │
   └────┬────┘   └──────────────┘
        │
        ▼
   ┌────────────────────────────┐
   │  Match Found!              │
   │  User: Andre Salvador      │
   │  Email: andre@carsu.edu.ph │
   └────┬───────────────────────┘
        │
        ├─→ Send EMAIL Notification
        │   "Your ID (221-01618) has been found!"
        │
        ├─→ Send SOCKET.IO Real-time
        │   Event: "id_number_found"
        │   Room: user_${userId}
        │
        └─→ Store in DB
            notifications table
            category: "id_number_match"


════════════════════════════════════════════════════════════════════
SCENARIO 2: SECURITY STAFF REPORTS FOUND ID
════════════════════════════════════════════════════════════════════

    ┌──────────────────────────────┐
    │  Security Staff Reports      │
    │  "Found ID: 221-01618"       │
    │  type: "found"               │
    │  Auto-accepted to custody    │
    └──────────┬────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────┐
    │  System Receives Report              │
    │  type: "found"                       │
    │  status: "in_security_custody"       │
    │  student_id: "221-01618"             │
    └──────────┬───────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────┐
    │  ID NUMBER MATCHING TRIGGER          │
    │  ✅ NEW FEATURE                      │
    └──────────┬───────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────┐
    │  Query Users Table                   │
    │  WHERE id_number = "221-01618"       │
    │  (case-insensitive)                  │
    └──────────┬───────────────────────────┘
               │
        ┌──────┴──────────────────┐
        │                         │
        ▼                         ▼
   ┌─────────┐            ┌──────────────┐
   │ FOUND   │            │ NOT FOUND    │
   └────┬────┘            └──────────────┘
        │
        ▼
   ┌─────────────────────────────────┐
   │  Match Found!                   │
   │  User: Andre Salvador           │
   │  Email: andre@carsu.edu.ph      │
   │  ID Number: 221-01618           │
   └────┬────────────────────────────┘
        │
        ├─→ CREATE NOTIFICATION
        │   └─ category: "id_number_match"
        │   └─ type: "id_number_found"
        │   └─ message: "Your ID has been found!"
        │
        ├─→ SEND EMAIL
        │   Subject: "Your ID Has Been Found"
        │   Body: Item details, location, who found it
        │
        ├─→ EMIT REAL-TIME EVENT
        │   Socket.io to: user_${userId}
        │   Event: newNotification
        │   Data: { item_id, category, type, message }
        │
        └─→ CHECK DUPLICATES
            Before creating notification:
            If notification exists → Skip
            If no notification → Create new


════════════════════════════════════════════════════════════════════
DATABASE MATCHING QUERY FLOW
════════════════════════════════════════════════════════════════════

    Input: student_id = "221-01618"
           (from reported item)
               │
               ▼
    ┌──────────────────────────────────────┐
    │  SELECT FROM users                   │
    │  WHERE id_number IS NOT NULL         │
    │  AND LOWER(id_number) = LOWER(?)     │
    │  LIMIT 1                             │
    └──────────┬───────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────┐
    │  Normalize Input (case-insensitive)  │
    │  "221-01618" → "221-01618" (lowercase)
    └──────────┬───────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────┐
    │  Search users table                  │
    │  Matching ID number                  │
    └──────────┬───────────────────────────┘
               │
        ┌──────┴──────┐
        │             │
        ▼             ▼
   ┌─────────┐   ┌──────────────┐
   │ Result  │   │ Result       │
   │ Found   │   │ Not Found    │
   │ User ID │   │ Return NULL  │
   │ Name    │   │              │
   │ Email   │   │              │
   └─────────┘   └──────────────┘


════════════════════════════════════════════════════════════════════
MATCHING LOGIC PRIORITY
════════════════════════════════════════════════════════════════════

┌─ LOST ITEM REPORTED ─┐
│                      │
├─→ 1️⃣  ID NUMBER MATCHING (NEW!)
│     └─ Check: Does any user have id_number = student_id?
│     └─ Action: Notify that user
│
├─→ 2️⃣  STUDENT ID MATCHING (for ID category)
│     └─ Check: Does any FOUND ID match this student_id?
│     └─ Action: Create match record, notify finder
│
└─→ 3️⃣  GENERAL ITEM MATCHING
      └─ Check: Brand + Color + Fuzzy Name
      └─ Action: Create match record, notify finder


┌─ FOUND ITEM REPORTED ─┐
│  (Staff / Custody)    │
│                       │
├─→ 1️⃣  ID NUMBER MATCHING (NEW!)
│     └─ Check: Does any user have id_number = student_id?
│     └─ Action: Notify that user immediately
│
├─→ 2️⃣  STUDENT ID MATCHING (for ID category)
│     └─ Check: Does any LOST ID match this student_id?
│     └─ Action: Create match record, notify reporter
│
└─→ 3️⃣  GENERAL ITEM MATCHING
      └─ Check: Brand + Color + Fuzzy Name
      └─ Action: Create match record, notify reporter


════════════════════════════════════════════════════════════════════
NOTIFICATION DELIVERY CHANNELS
════════════════════════════════════════════════════════════════════

    ┌─ Match Detected ─┐
    │                  │
    ├─→ 📧 EMAIL
    │   ├─ To: matched_user@email.com
    │   ├─ Subject: "Your ID Has Been Found"
    │   └─ Body: Details about found item
    │
    ├─→ 🔔 SOCKET.IO (Real-time)
    │   ├─ Room: user_${userId}
    │   ├─ Event: newNotification
    │   └─ Type: id_number_found
    │
    └─→ 💾 DATABASE
        ├─ Table: notifications
        ├─ Category: id_number_match
        └─ Visible in user dashboard


════════════════════════════════════════════════════════════════════
DUPLICATE PREVENTION FLOW
════════════════════════════════════════════════════════════════════

    Match Found: user_id=UUID, item_id=UUID
               │
               ▼
    ┌──────────────────────────────┐
    │ Check if notification exists │
    │ WHERE user_id = UUID         │
    │   AND category = "id_number" │
    │   AND item_id = UUID         │
    └──────────┬────────────────────┘
               │
        ┌──────┴──────┐
        │             │
        ▼             ▼
   ┌─────────┐   ┌──────────────┐
   │ EXISTS  │   │ NOT EXISTS   │
   │ SKIP    │   │ CREATE NEW   │
   │ NO DUP  │   │ NOTIFICATION │
   └─────────┘   └──────────────┘


════════════════════════════════════════════════════════════════════
DATA FLOW: USER PROFILE → MATCHING → NOTIFICATION
════════════════════════════════════════════════════════════════════

Step 1: User Sets Up Profile
        ┌─────────────────────┐
        │ UserProfile.vue     │
        │ ID Number: 221-01618│ ← User enters format: XXX-XXXXX
        └──────────┬──────────┘
                   │
                   ▼
        ┌─────────────────────┐
        │ users TABLE         │
        │ id_number: 221-01618│ ← Stored in database
        └──────────┬──────────┘
                   │
Step 2: Item Gets Reported
        ├─→ ┌─────────────────────┐
            │ items TABLE         │
            │ student_id: 221-01618│ ← From report form
            └──────────┬──────────┘
                       │
Step 3: Matching Triggered
        ├─→ ┌──────────────────────────┐
            │ reportRoutes.js          │
            │ ID NUMBER MATCHING CHECK │ ← NEW FEATURE
            └──────────┬───────────────┘
                       │
Step 4: Query & Match
        ├─→ ┌──────────────────────────────────┐
            │ SELECT * FROM users              │
            │ WHERE id_number = "221-01618"    │
            │ Result: Match found (user record)│
            └──────────┬───────────────────────┘
                       │
Step 5: Create Notification
        ├─→ ┌──────────────────────┐
            │ notifications TABLE  │
            │ category: id_number  │
            │ user_id: USER_UUID   │
            └──────────┬───────────┘
                       │
Step 6: Send Alerts
        ├─→ 📧 Email Service
        ├─→ 🔔 Socket.io Event
        └─→ 💾 Dashboard Display
```

---

## 🔑 Key Implementation Points

### 1. **When Matching Occurs**
- ✅ When ANY lost item is reported (if student_id provided)
- ✅ When ANY found item is accepted to custody by staff (if student_id provided)

### 2. **What Gets Matched**
- 🆔 User's `id_number` field
- 📝 Reported item's `student_id` field
- 🔤 Case-insensitive comparison

### 3. **Who Gets Notified**
- 👤 The user whose ID number matches
- 📧 Via email + real-time socket.io
- 🔔 With duplicate prevention

### 4. **Why This Matters**
- ⏱️ Instant notification instead of waiting for item report
- 🎯 Direct match with user profile data
- 🚀 Faster resolution of lost items
- 😊 Better user experience

---

## 📊 Example Scenarios

### Scenario A: Quick Resolution
```
9:00 AM - Student loses ID (has "221-01618" in profile)
9:05 AM - Someone finds ID and reports to security
9:05 AM - ✅ Student receives notification immediately!
         "Your ID (221-01618) has been found!"
9:15 AM - Student claims ID from security office
```

### Scenario B: Case Insensitivity
```
Profile stores:  "221-01618"
Found item:      "221-01618" (different case)
Result:          ✅ Still matches!
Notification:    Sent to user
```

### Scenario C: Duplicate Prevention
```
11:00 AM - Found item reported with ID "221-01618"
           → Notification #1 sent to user
11:05 AM - Same item details reviewed again
           → System checks for duplicate
           → No duplicate notification (prevented!)
```

---

**Last Updated:** December 12, 2025
